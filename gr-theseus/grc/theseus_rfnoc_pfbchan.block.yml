# auto-generated by grc.converter

id: theseus_rfnoc_pfbchan
label: 'RFNoC: PFB Channelizer'

parameters:
-   id: num_chan
    label: Number of Channels
    dtype: int
    default: '8'
-   id: chan_idx
    label: Channel Indices
    dtype: int_vector
    default: '[0, 1]'
    hide: part
-   id: vec_len_in
    label: Input Vector Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: block_index
    label: Block Select
    category: RFNoC Config
    dtype: int
    default: '-1'
    hide: ${ ('part' if int(block_index) < 0 else 'none') }
-   id: device_index
    label: Device Select
    category: RFNoC Config
    dtype: int
    default: '-1'
    hide: ${ ('part' if int(device_index) < 0 else 'none') }
-   id: fpga_module_name
    label: FPGA Module Name
    category: RFNoC Config
    dtype: string
    default: noc_block_pfbchan
    hide: all

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vec_len_in }

outputs:
-   domain: stream
    dtype: complex
    vlen: 1
    multiplicity: ${ len(chan_idx) }

templates:
    imports: import theseus
    make: |
        theseus.pfbchan(
            self.device3,
            ${block_index},
            ${device_index},
            ${num_chan},
            ${chan_idx}
        )

file_format: 1
